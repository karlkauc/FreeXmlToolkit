name: Build and Release Executables

# Dieser Workflow wird bei jedem neuen Release gestartet
on:
  release:
    types: [ created ]

jobs:
  build:
    # Definiere eine Build-Matrix für verschiedene Betriebssysteme und Architekturen
    strategy:
      matrix:
        include:
          # Windows builds
          - os: windows-latest
            arch: x64
            gradle-tasks: "createWindowsX64Exe createWindowsX64Msi createWindowsX64AppImage"
          - os: windows-latest
            arch: arm64
            gradle-tasks: "createWindowsArm64Exe createWindowsArm64Msi createWindowsArm64AppImage"
          
          # macOS builds
          - os: macos-13  # Intel runners
            arch: x64
            gradle-tasks: "createMacOSX64Dmg createMacOSX64Pkg createMacOSX64AppImage"
          - os: macos-latest  # ARM runners
            arch: arm64
            gradle-tasks: "createMacOSArm64Dmg createMacOSArm64Pkg createMacOSArm64AppImage"
          
          # Linux builds
          - os: ubuntu-latest
            arch: x64
            gradle-tasks: "createLinuxX64Deb createLinuxX64Rpm createLinuxX64AppImage"
          - os: ubuntu-latest
            arch: arm64
            gradle-tasks: "createLinuxArm64Deb createLinuxArm64Rpm createLinuxArm64AppImage"

    # Der Job wird auf dem in der Matrix definierten Betriebssystem ausgeführt
    runs-on: ${{ matrix.os }}

    steps:
      # Schritt 1: Code auschecken
      - name: Checkout repository
        uses: actions/checkout@v4

      # Schritt 2: Java Development Kit (JDK) einrichten
      # Stelle sicher, dass du eine JDK-Version wählst, die jpackage enthält (JDK 14+)
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-package: 'jdk+fx'
          java-version: '24'
          distribution: 'liberica'

      # Schritt 3: Gradle Wrapper ausführbar machen (wichtig für Linux/macOS)
      - name: Make Gradle wrapper executable
        if: runner.os != 'Windows'
        run: chmod +x ./gradlew

      # Schritt 4: Führe die architektur-spezifischen Gradle-Tasks aus
      - name: Build with Gradle (${{ matrix.arch }})
        run: ./gradlew ${{ matrix.gradle-tasks }}

      # Schritt 5: Lade alle erstellten Artefakte mit dem GitHub CLI hoch
      - name: Upload Release Assets
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload ${{ github.event.release.tag_name }} build/dist/*
